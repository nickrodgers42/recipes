import { makeStyles } from '@material-ui/styles'

const useStyles = makeStyles({
  divider: {
    width: 2,
    height: 28,
    margin: 4
  },
  searchField: {
    padding: '2px 4px',
    display: 'flex',
    alignItems: 'center',
    height: 36,
  },
  searchInput: {
    marginLeft: 8,
    flex: 1
  }
})
export default useStyles
